<UserControl x:Class="superAwesomeAmazingTourPlanner.Views.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ViewModels="clr-namespace:ViewModels.ViewModels;assembly=ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <StackPanel Grid.Row="0" Grid.Column="0" Background="{StaticResource BackgroundColor}">
            <TextBlock
                Style="{StaticResource Title}"
                Text="{Binding Title}"
                />

        <Image Source="{Binding Logo, Converter={StaticResource PathToBitMapImageConverter}}" Stretch="None"/>
    </StackPanel>

</UserControl>
